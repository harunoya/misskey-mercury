# Misskey Mercuryについて

Misskey Mercuryは、[Misskey](https://github.com/misskey-dev/misskey)をもとに、CherryPickからの移行支援、追加テーマ、Matrixクライアントなどを組み込んだフォークです。

個人利用を前提に保守しており、Misskey公式プロジェクトとはサポート窓口が異なります。
利用前に、この文書に記載した制限とデータの扱いを確認してください。

最終更新日：2026年8月24日

## 主な違い

| 機能 | 内容 | 主な対象 |
| --- | --- | --- |
| Matrixクライアント | ダイレクトメッセージ画面からMatrixへ接続 | 利用者 |
| 追加テーマ | 上流にない28種類の組み込みテーマを追加 | 利用者 |
| ログイン画面 | 日本語の挨拶を複数の文面から表示 | 利用者 |
| CherryPick移行 | CherryPickのデータベースをMisskeyへ移す処理を追加 | サーバー管理者 |
| パスワード互換 | CherryPick由来のArgon2idハッシュでログイン可能 | 移行した利用者、サーバー管理者 |
| バージョン識別 | Mercury用のバージョン接尾辞とソースコードリンクを表示 | サーバー管理者 |

## Matrixクライアント

ダイレクトメッセージ画面の「Matrix」タブから、Matrixホームサーバーへ接続できます。

通信はMisskeyサーバーを経由しません。
ブラウザが、入力したホームサーバーへ直接接続します。

### 対応している機能

- Matrix IDまたはユーザー名とパスワードによるログイン
- 複数アカウントの追加と切り替え
- 異なるホームサーバー間の切り替え
- アカウントごとのログアウト
- 参加中のルーム一覧とルーム検索
- 未読数とルームトピックの表示
- ダイレクトルームの作成
- テキストメッセージの表示と送信
- 既読状態の送信
- 自動同期と手動再同期
- デスクトップ向けの2ペイン表示
- スマートフォン向けの画面切り替え表示

同期中も取得済みのルーム一覧は表示されたままになります。
メッセージ送信欄は画面下部へ固定され、入力内容に応じて高さが変わります。

### アカウント情報の保存

Matrixのアクセストークンは、使用中のブラウザのローカルストレージへ保存されます。
Misskeyサーバーのデータベースには保存されません。

ログイン時のパスワードと送受信するメッセージは、選択したMatrixホームサーバーへ直接送信されます。
パスワードはローカルストレージへ保存しません。

共有端末では利用後にMatrixからログアウトしてください。
ブラウザのプロフィールを共有している場合、同じプロフィールを使う人が保存済みセッションへアクセスできる可能性があります。

ログアウトすると、選択中のMatrixアカウントだけをブラウザから削除します。
ほかの保存済みアカウントは残り、次のアカウントへ自動的に切り替わります。

### 接続条件

ホームサーバーは、ブラウザからのCORSアクセスを許可している必要があります。
CORSが許可されていないホームサーバーには、このクライアントから接続できません。

現在はパスワード認証だけに対応しています。
SSO、外部認証、Matrixアカウントの新規登録には対応していません。

### 未対応の機能

次の機能は利用できません。

- 暗号化ルームのメッセージ復号と送信
- 画像やファイルなどの添付
- リアクション
- メッセージの編集と削除
- 返信とスレッド
- 過去の履歴の追加読み込み

暗号化ルームは一覧へ表示されますが、読み書きは無効になります。
各ルームで画面上に保持するメッセージは直近300件までです。

## 追加テーマ

Mercuryは、上流のMisskeyにない次のテーマを組み込んでいます。

- Birdsite Pure：Dark、white
- Byeolvit：Noctiluca Rev.1、Polaris Rev.1
- CherryPick：Dark、Light
- Metal：Black Chrome Dark、Bronze Dark、Cobalt Steel Dark、Copper Dark、Gunmetal Dark
- Metal：Champagne Gold Light、Platinum Light、Rose Gold Light、Silver Light、Titanium Light
- Mi Red：Dark、Light
- Mirerado：Neptune、Uranus
- Qdon
- Rosé Pine：Rosé Pine、Rosé Pine Moon、Rosé Pine Dawn
- scone.color：scone.color 0.0.1、buttersconedefault
- Stella R2：Night、Light

テーマは設定画面から選択できます。
ライトテーマとダークテーマは、テーマ名と配色を確認して選んでください。

## CherryPickからの移行

この節はサーバー管理者向けです。

Mercuryには、CherryPickで使っていたデータベースを現在のMisskeyスキーマへ移す処理が含まれています。
移行処理はCherryPick固有のカラム、テーブル、列挙型を削除し、Misskeyで必要な構造へ調整します。

### 移行前の注意

この移行処理は元に戻せません。
CherryPick固有のデータを削除するため、適用前にデータベース全体のバックアップを作成してください。

CherryPickのユーザーグループを参照するアンテナは、Misskey側に対応する参照先がないため削除されます。
CherryPick固有機能のデータを残す必要がある場合は、移行を始める前に別の形式で保管してください。

本番環境へ適用する前に、バックアップから複製した検証環境でデータベース移行と起動を確認してください。

### パスワードの扱い

CherryPickから移行したアカウントは、既存のArgon2idパスワードハッシュを使ってログインできます。

新しく設定したパスワードにはbcryptを使用します。
管理画面で「CherryPick」と表示されたアカウントには、パスワードを再設定してbcryptへ更新するよう案内してください。

## バージョンとソースコードの表示

サーバー管理者は、ビルド時に`MISSKEY_VERSION_SUFFIX`を指定してMercury用の接尾辞をバージョンへ追加できます。

たとえば、元のバージョンが`2026.8.0-alpha.0`で接尾辞が`mercury`なら、表示されるバージョンは`2026.8.0-alpha.0-mercury`になります。
接尾辞には英数字、ピリオド、ハイフンを使用できます。

「Misskeyについて」画面には、上流のMisskeyとMercuryのソースコードリンクを表示します。
インスタンスがMercury以外のリポジトリを指定している場合は、別の改変版としてそのソースコードも案内します。

## そのほかの表示変更

日本語のパスワードログイン画面では、「おかえりなさい」に相当する10種類の文面から一つを表示します。
他言語では上流のMisskeyと同じ文面を使用します。

日本語UIでは、二要素認証、パスワード強度、リレー追加、登録時のメールアドレス要件など、一部の表現を調整しています。

## 不具合の報告先

Mercury固有機能の不具合は、[Misskey Mercuryのリポジトリ](https://github.com/harunoya/misskey-mercury)で確認してください。

Matrixの接続エラーを報告する場合は、使用したホームサーバー、ブラウザ名、表示されたエラー内容を添えてください。
アクセストークン、パスワード、Cookieなどの認証情報は投稿しないでください。

上流のMisskeyにも存在する問題は、再現条件を確認したうえで[Misskey公式リポジトリ](https://github.com/misskey-dev/misskey)の案内に従ってください。

## 謝辞

MercuryはMisskeyをもとに開発されています。
移行互換機能と一部テーマには、[CherryPick](https://github.com/kokonect-link/cherrypick)の実装と成果物を取り入れています。

各ソフトウェアとテーマの著作者、貢献者に感謝します。
